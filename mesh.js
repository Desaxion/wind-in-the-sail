class Mesh {
    constructor(vertices, vertexNormals, faces, texCoords){
        this.vertices = vertices;
        this.vertexNormals = vertexNormals;
        this.faces = faces;
        this.texCoords = texCoords;
    }
}